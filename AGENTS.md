# Agent Rules

Telegraph style.

Coding Constraint

- **应**在编码完成后维护wiki

## Project Knowledge

- 项目相关内容（例如架构、启动方式、编码规范等）均在 `wiki/` 目录中，入口: `wiki/index.md` ，按需渐进式检索，无需全量扫描。

## Map

- `tmp-files/`：临时文件/中间产物，不提交 git


## User Interaction

- 向用户提问时，每次仅提出一个问题，待用户回答后再继续下一个。**禁止** 批量提问。

## Documentation Language Style

- 文档默认使用中文表达；专业技术术语、名称、标识符和代码相关内容保持原文。
- 引用已有上下文中的名称、标识符和技术术语时，保持原文写法。
- 同一术语在同一文档中保持一致。
- 代码块、命令、路径、变量名、字段名、配置项和示例输出保持原样。