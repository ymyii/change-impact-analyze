/**
 * Diagnostic topology, node identity, rank, path, and IR snapshots.
 * May depend on public engine metadata and common model contracts.
 * Must not depend on concrete strategies, impact, or report types.
 */
package io.github.dependencyanalysis.callgraph.topology;
