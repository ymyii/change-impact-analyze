/** Algorithm-independent protocol limitations, kinds, and result contracts.
 * May depend on immutable Call Graph model and local-analysis contracts.
 * Must not depend on strategies, the engine, impact, or report types.
 */
package io.github.dependencyanalysis.callgraph.protocol;
