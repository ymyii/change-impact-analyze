/**
 * Call Graph algorithm identifiers, policies, contracts, and build context.
 * May depend on common scope, entrypoint, protocol, and model contracts.
 * Only the factory may register concrete strategies.
 * Must not depend on the engine, impact, or report.
 */
package io.github.dependencyanalysis.callgraph.strategy;
