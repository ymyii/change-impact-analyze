/**
 * Class ownership, classpath sources, validation, and scope projection.
 * May depend on immutable Call Graph model contracts.
 * Must not depend on strategies, impact, or report types.
 */
package io.github.dependencyanalysis.callgraph.scope;
