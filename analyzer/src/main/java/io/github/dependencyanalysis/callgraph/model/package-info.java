/** Immutable identities and values shared across Call Graph subsystems.
 * May depend only on lower-level platform and library types.
 * Must not depend on engines, strategies, impact, or report types.
 */
package io.github.dependencyanalysis.callgraph.model;
